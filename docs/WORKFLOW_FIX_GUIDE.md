# n8n 워크플로우 완전 수정 가이드

## 🚨 문제 상황

1. **Supabase Update 노드가 실행되지만 웹에서 진행 상태가 변하지 않음**
2. **에러 발생 시 웹이 계속 "진행 중" 상태로 멈춤**

## 🔍 근본 원인

### 원인 1: ad_video_id 데이터 유실

n8n에서 노드를 거칠 때마다 데이터 구조가 변경됩니다:

```
Webhook → { body: { ad_video_id: "uuid", ... } }
  ↓
AI 처리 노드 → { 완전히 다른 데이터 구조 }
  ↓
Merge 노드 → { 두 입력 결합, ad_video_id 없음! }
  ↓
Update 노드 → ❌ ad_video_id = undefined
```

**결과**: Supabase Update가 실패하거나 잘못된 row를 업데이트

### 원인 2: Webhook body 구조

웹에서 보낸 데이터:
```json
{
  "ad_video_id": "uuid",
  "product_image_id": "uuid",
  "product_info_id": "uuid"
}
```

n8n Webhook이 받은 데이터:
```json
{
  "headers": {...},
  "body": {
    "ad_video_id": "uuid",
    "product_image_id": "uuid",
    "product_info_id": "uuid"
  }
}
```

**중요**: `$json.ad_video_id`가 아니라 `$json.body.ad_video_id`로 접근해야 합니다!

## ✅ 해결 방법

### 방법 1: Set 노드로 데이터 보존 (권장)

각 AI 처리 노드 **직후**에 "Set" 노드를 추가하여 `ad_video_id`를 보존:

```
AI 처리 노드
  ↓
Set 노드 (ad_video_id 추가)
  ↓
Update 노드 (정상 작동!)
```

### 방법 2: Webhook 직접 참조

모든 Update 노드에서 Webhook을 직접 참조:

```javascript
={{ $('Webhook').item.json.body.ad_video_id }}
```

**하지만** 이 방법은 워크플로우가 복잡해지면 실패할 수 있습니다.

## 🛠️ 실전 수정 단계

### Step 1: Webhook 직후 데이터 추출

**"Respond to Webhook1" 노드 바로 다음**에 "Set" 노드 추가:

1. n8n 워크플로우 편집 화면
2. "Respond to Webhook1"과 "Update: Ad Copy Generation" 사이에 **"Set" 노드** 추가
3. Set 노드 이름: **"Extract Workflow Data"**
4. 다음 값 설정:

```javascript
// ad_video_id
Name: ad_video_id
Value: ={{ $json.body.ad_video_id }}

// product_image_id
Name: product_image_id
Value: ={{ $json.body.product_image_id }}

// product_info_id
Name: product_info_id
Value: ={{ $json.body.product_info_id }}
```

**연결 구조:**
```
Webhook
  ↓
Respond to Webhook1
  ↓
Extract Workflow Data (Set 노드) ← 새로 추가!
  ↓
Update: Ad Copy Generation
```

### Step 2: 모든 Update 노드 수정

이제 **모든 Update 노드**의 filter를 다음과 같이 수정:

#### 현재 (잘못된 방식):
```javascript
keyValue: ={{ $json.ad_video_id }}
```

#### 수정 후 (올바른 방식):
```javascript
keyValue: ={{ $('Extract Workflow Data').item.json.ad_video_id }}
```

**수정해야 할 노드 목록:**
1. ✅ Update: Ad Copy Generation
2. ✅ Update: Image Refinement
3. ✅ Update: Video Generation
4. ✅ Update: TTS Generation (있다면)
5. ✅ Update: Subtitle Generation (있다면)
6. ✅ Update: Merging (있다면)
7. ✅ Update: Completed (있다면)

### Step 3: AI 처리 노드 후 데이터 보존

각 **AI 처리 노드 직후**에 Set 노드 추가하여 ad_video_id 유지:

#### 예시: 광고문구 생성기 이후

**현재 구조:**
```
광고문구 생성기
  ↓
Merge 또는 다른 노드
```

**수정 후:**
```
광고문구 생성기
  ↓
Preserve Ad Video ID (Set 노드) ← 새로 추가!
  ↓
Merge 또는 다른 노드
```

**Set 노드 설정:**
```javascript
// 기존 AI 출력 데이터 유지 (전체 복사)
Keep: true
Mode: Manual

// ad_video_id 추가
Name: ad_video_id
Value: ={{ $('Extract Workflow Data').item.json.ad_video_id }}
```

### Step 4: 에러 처리 추가

워크플로우 **마지막**에 Error Trigger 노드 추가:

1. **Error Trigger** 노드 추가
2. 연결: 전체 워크플로우의 에러를 캐치
3. Error Trigger 다음에 **Update Error Status** (Supabase 노드) 추가

**Update Error Status 설정:**
```javascript
Operation: update
Table: ad_videos

Filter:
  keyName: id
  condition: eq
  keyValue: ={{ $json.ad_video_id || $('Extract Workflow Data').item.json.ad_video_id }}

Fields to Update:
  status: failed
  progress_stage: error
  error_message: ={{ $json.error.message }}
```

## 📋 전체 워크플로우 구조 (수정 후)

```
┌─────────────────────────────────────────────────────┐
│ 1. Webhook 수신                                      │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 2. Respond to Webhook1 (즉시 응답)                  │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 3. Extract Workflow Data (Set)                      │
│    - ad_video_id, product_image_id, product_info_id │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 4. Update: Ad Copy Generation                       │
│    filter: $('Extract Workflow Data').ad_video_id   │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 5. Get Product Info (Supabase)                      │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 6. 광고문구 생성기 (AI)                              │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 7. Preserve Ad Video ID (Set)                       │
│    - 기존 출력 + ad_video_id 추가                    │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ 8. Update: Image Refinement                         │
│    filter: $('Extract Workflow Data').ad_video_id   │
└─────────────────────────────────────────────────────┘
                      ↓
                   (반복)

                      ↓
┌─────────────────────────────────────────────────────┐
│ Error Trigger (에러 발생 시)                         │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│ Update Error Status                                 │
│    status: failed, progress_stage: error            │
└─────────────────────────────────────────────────────┘
```

## 🎯 핵심 포인트

### ✅ DO (해야 할 것)

1. **Webhook 직후 데이터 추출**: Set 노드로 `ad_video_id`, `product_image_id`, `product_info_id` 추출
2. **모든 Update 노드에서 동일한 참조**: `$('Extract Workflow Data').item.json.ad_video_id`
3. **AI 노드 후 데이터 보존**: Set 노드로 `ad_video_id` 계속 전달
4. **에러 처리**: Error Trigger로 실패 시 DB 상태 업데이트

### ❌ DON'T (하지 말아야 할 것)

1. **`$json.ad_video_id` 사용 금지**: Merge 이후 undefined 됨
2. **`$json.body.ad_video_id` 직접 사용 금지**: Webhook 이후 노드에서는 body가 없음
3. **에러 처리 없이 방치**: 웹이 무한 로딩 상태로 멈춤

## 🧪 테스트 방법

### 1. n8n Execution 로그 확인

워크플로우 실행 후:
1. n8n → Executions 탭
2. 최근 실행 클릭
3. 각 노드 클릭하여 **Output Data** 확인

**확인할 것:**
- Extract Workflow Data 출력: `ad_video_id`, `product_image_id`, `product_info_id`가 있는지
- 각 Update 노드 입력: `id` filter가 올바른 UUID인지
- AI 노드 후 Set 노드: `ad_video_id`가 보존되는지

### 2. Supabase에서 직접 확인

```sql
-- 실시간으로 progress_stage 변화 확인
SELECT id, status, progress_stage, updated_at
FROM ad_videos
ORDER BY created_at DESC
LIMIT 5;

-- 3초마다 새로고침하여 변화 확인
```

### 3. 웹 UI 확인

브라우저 개발자 도구 → Console:
```javascript
// Realtime 구독 로그가 찍히는지 확인
// 각 단계마다 UPDATE 이벤트가 와야 함
```

## 🔧 문제 해결

### 문제: "여전히 웹에서 진행 상태가 안 변함"

**확인 사항:**
1. n8n Execution 로그에서 Update 노드들이 **성공**했는지
2. Supabase에서 `progress_stage`가 실제로 **변경**되었는지
3. 웹 브라우저 Console에서 Realtime **구독 로그** 확인

**가능한 원인:**
- RLS 정책이 service_role 업데이트를 막고 있음
- Realtime이 비활성화되어 있음
- 웹에서 잘못된 `ad_video_id`를 구독 중

### 문제: "Update 노드에서 계속 에러"

**에러: `invalid input syntax for type uuid: "undefined"`**

**원인**: `ad_video_id`가 여전히 undefined

**해결:**
1. Extract Workflow Data 노드가 **Respond to Webhook1 직후**에 있는지 확인
2. Extract Workflow Data의 expression: `={{ $json.body.ad_video_id }}`
3. Update 노드의 expression: `={{ $('Extract Workflow Data').item.json.ad_video_id }}`

### 문제: "에러 발생 후 웹이 멈춤"

**원인**: Error Trigger가 없음

**해결:**
1. Error Trigger 노드 추가
2. Update Error Status 노드로 `status: failed` 업데이트
3. 웹에서 `status === 'failed'` 처리 로직 확인

## 📊 Expression 치트시트

| 노드 위치 | 올바른 Expression |
|-----------|-------------------|
| Extract Workflow Data (Webhook 직후) | `={{ $json.body.ad_video_id }}` |
| 모든 Update 노드 | `={{ $('Extract Workflow Data').item.json.ad_video_id }}` |
| AI 노드 후 Set (데이터 보존) | `={{ $('Extract Workflow Data').item.json.ad_video_id }}` |
| Error Trigger 후 | `={{ $json.ad_video_id || $('Extract Workflow Data').item.json.ad_video_id }}` |

## 🎬 다음 단계

1. ✅ 위 가이드대로 워크플로우 수정
2. ✅ 저장 및 활성화
3. ✅ 웹에서 테스트
4. ✅ n8n Execution 로그 확인
5. ✅ Supabase에서 변경 확인
6. ✅ 웹 UI에서 실시간 진행 상태 확인

모든 단계가 정상 작동하면 완료!